// @import 'nib'

//color scheme
//inspired by the black white and teal colors for Uber
black = #0e0e0e;
grey = #272727;
lightTeal = #1fbad6;
mint = #38b0af;
red = #BD4242;
darkRed = #790D0D;

//base case, mobile first dimensions
font_size = 18;
field_width = 300;
box_spacing = 5;
tile_size = (field_width / 9) - box_spacing;

html, body {
  font-size: font_size px; 
  position:relative;
  margin: 0;
  padding: 0;
  color: black;
  font-family: "Clear Sans", "Helvetica Neue", Arial, sans-serif;
}
a:visited{
  color:lightTeal;
}
.container{
  width: field_width px;
  margin-left: auto;
  margin-right: auto;
}
// #headline{
//   width: field_width px;
//   margin-left: auto;
//   margin-right: auto;
// }

header{
  color: black;
  text-align: center;
}

//CSS trick to have faded background text. 
.backgroundText{
  margin-top: -(font_size*5) px;
  font-size: font_size*3 px;
  color: gray;
  opacity: 0.2;
  z-index:-1;
  text-align: center;
}

#gameboard{
  width: field_width px;
  height: field_width px;
  margin-left: auto;
  margin-right: auto;
  
  table{
    border-collapse: separate;
    cellspacing: 0;
    background-color:transparent;
  }

  table table td, input{
    width:tile_size px;
    height:tile_size px;
    font-size:font_size px;
    border-spacing: box_spacing px;
    overflow: hidden;
    border-collapse: separate;
    background-color: lightTeal;
    margin:0;
    padding:0;
    border:0;
    color:white;
    text-align: center;
    vertical-align: middle;
    font-family: "Clear Sans", "Helvetica Neue", Arial, sans-serif;
  }

  input{
    background-color: transparent;
  }

  input[type=number]::-webkit-inner-spin-button, 
  input[type=number]::-webkit-outer-spin-button { 
    -webkit-appearance: none;
  }

  .static {
    background-color: black;
    color: white;
  }

  table .conflict{
    background-color: darkRed;
  }

  .conflict td{
    background-color:red;
    .static{
      background-color: black;
    }
  } 

}

#controls{
  width: field_width px;
  padding:box_spacing px;
  margin-left: auto;
  margin-right: auto;
  button{
    height: font_size * 2 px;
    margin: 1%;
    display: flex-box;
    border: 2px solid #1fbad6
    color: white;
    font-family: "Clear Sans", "Helvetica Neue", Arial, sans-serif;
    font-size: font_size px;
    border-radius: 4px;
    width: 45.5%;
    vertical-align: middle;
    background-color: black;
    text-align: center;    
  }
}

footer{
  width: field_width px;
  margin-left: auto;
  margin-right: auto;
  font-size : font_size px;
  a i{
    font-size: font_size*1.5 px;
  }
}

// Handle css for larger screen sizes.
font_size = 18*2;
field_width = 500;
box_spacing = 5;
tile_size = (field_width / 9) - box_spacing;

@media (min-width: 500px){
  html, body {
    font-size: font_size px; 
  }
  .backgroundText{
    margin-top: -(font_size*5) px;
    font-size: font_size*3 px;
  }
  #gameboard{
    width: field_width px;
    height: field_width px;
    table table td, input{
      width:tile_size px;
      height:tile_size px;
      font-size:font_size px;
      border-spacing: box_spacing px;
    }
  }
  .container{
    width: field_width px;
  }
  #controls{
    width: field_width px;
    padding:box_spacing px;
    button{
      height: font_size * 2 px;
    }
  }
}